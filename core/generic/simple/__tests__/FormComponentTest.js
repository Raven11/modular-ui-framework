import FormComponent from '../FormComponent';


describe('while rendering Form Component', () => {
  it('renders LoadingIndicator as part of form if state is LOADING, otherwise it doesnt render LoadingIndicator', () => {

  });

});
